playwright>=1.40
jdatetime>=4.0
pymupdf>=1.23

# System deps (apt, reinstall each session — NOT pip):
#   chromium libs: libnspr4 libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2
#     libxkbcommon0 libatspi2.0-0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2
#     libgbm1 libpango-1.0-0 libcairo2 libasound2
#   emoji rendering in cards: fonts-noto-color-emoji
# Run `sudo apt-get update` first (stale index → 404).
# Then: python3 -m playwright install chromium
